{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["token"],
  "properties": {
    "token": {
      "type": "string",
      "description": "GitHub Personal Access Token or bot token"
    },
    "owner": {
      "type": "string",
      "description": "GitHub username or organization name (extracted from location if not set)"
    },
    "repo": {
      "type": "string",
      "description": "Specific repository name (optional, defaults to all repos for the owner)"
    }
  }
}
